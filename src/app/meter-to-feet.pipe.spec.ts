import { MeterToFeetPipe } from './meter-to-feet.pipe';

describe('MeterToFeetPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterToFeetPipe();
    expect(pipe).toBeTruthy();
  });
});
